<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= post.title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <!--READ FULL POST IN NEW PAGE. TIES TO INDEX.JS LINE 29 -->
    <div class="container w-50 text-center">
      <h2><%= post.title %></h2>
      <p><%= post.entry %></p>
      <!--ON FORM SUBMISSION EDIT, SERVER MOVES TO edit.ejs TIES TO INDEX.JS LINE 46 -->
      <form
        action="/edit/<%= encodeURIComponent(post.title) %>"
        method="GET"
        class="form-control-lg"
      >
        <input type="submit" value="Edit Post" class="btn btn-primary mb-2" />
      </form>
      <!--ON FORM SUBMISSION DELETE, SERVER REMOVES FROM ARRAY & REDIRECTS TO /POST-INDEX. TIES TO INDEX.JS LINE 39 -->
      <form
        action="/delete/<%= encodeURIComponent(post.title) %>?_method=DELETE"
        method="post"
      >
        <input type="submit" value="Delete Post" class="btn btn-primary mb-2" />
      </form>

      <br />
      <hr />
      <a href="/post-index">Back to Post Index</a>
    </div>
  </body>
</html>
